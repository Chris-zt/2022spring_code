#include "Base.h"

class Solution
{
private:
	int m = 0, n = 0;
public:
	vector<int> luckyNumbers(vector<vector<int>> &matrix)
	{
		m = matrix.size(), n = matrix[0].size();
		vector Lucky
	}
};